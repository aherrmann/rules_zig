load("@rules_zig//zig:defs.bzl", "zig_package", "zig_test")

zig_test(
    name = "direct-data",
    data = ["data.txt"],
    main = "direct-data.zig",
)

zig_package(
    name = "package",
    data = ["data.txt"],
    main = "package.zig",
)

zig_test(
    name = "package-data",
    main = "package-data.zig",
    deps = [":package"],
)
